// import emojiList from '../assets/emoji';

const textWithEmoji = (text: string) => {
  // const rex = RegExp(/:[^:s]*(?:::[^:s]*)*:/, 'i');
  // const updated = text.replace(rex, match => {
  //   return emojiList[match.slice(1, -1)];
  // });
  // console.log(updated);
  return text;
};

export default textWithEmoji;
